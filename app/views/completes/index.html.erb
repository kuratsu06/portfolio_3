<div class="top_page">
  <% @completes.each do |complete| %>
    <div class="top_img">
      <%= link_to complete_path(complete) do %>
        <% if complete.image? %>
          <%= image_tag complete.image.thumb_2.url %>
        <% else %>
          <p class="no_img">no image</p>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>
